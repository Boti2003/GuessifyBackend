namespace GuessifyBackend.Models.Enum
{
    public enum JoinStatus
    {
        SUCCESS,
        LOBBY_FULL,
        LOBBY_NOT_FOUND,
        USER_ALREADY_IN_LOBBY,
        USER_HOST_GAME,
        USERNAME_TAKEN
    }
}
